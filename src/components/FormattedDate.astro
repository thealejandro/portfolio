---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('gt-GT', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
